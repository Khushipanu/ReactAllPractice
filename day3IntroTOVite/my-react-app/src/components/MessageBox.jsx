export default function MessageBox({userName,textColor}){
    // let styles={color:textColor}
    return(
        <>
        <h1 style={{color:"blue"}} >Hello, {userName}</h1>
        <p style={{color:textColor}}>im khushi</p>
        </>
    )

}